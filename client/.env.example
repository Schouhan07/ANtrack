# Optional: only for split deploy (frontend on Vercel/Netlify, API elsewhere).
# Example: https://api.yourdomain.com — no trailing slash.
# Leave unset when the Express server also serves the React build (same origin).
# REACT_APP_API_URL=
